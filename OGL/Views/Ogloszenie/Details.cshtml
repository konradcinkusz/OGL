@using Microsoft.AspNet.Identity;
    @model Repozytorium.Models.Ogloszenie

    @{
        ViewBag.Title = "Dane szczegółowe";
        ViewBag.Opis = "Opis danych szczegółowych";
        ViewBag.SlowaKluczowe = "Ogloszenie, " + Model.ID + ", szczegóły";
    }

    <h2>Details</h2>

    <div>
        <h4>Ogloszenie</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Uzytkownik.Email)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.UzytkownikId)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Tresc)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Tresc)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Tytul)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Tytul)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DataDodania)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DataDodania)
            </dd>

        </dl>
    </div>
    <p>
        @if (User.Identity.IsAuthenticated && User.IsInRole("Admin") || User.IsInRole("Pracownik") || Model.UzytkownikId == User.Identity.GetUserId())
        {
            <br />
            @Html.ActionLink("Edycja", "Edit", new { id = Model.ID }, new { @class = "btn btn-primary" }))
    }
        @Html.ActionLink("Powrót do listy", "Index", new { @class = "btn btn-warning" })
    </p>
